import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF2563EB);
  static const Color primaryLight = Color(0xFF3B82F6);
  static const Color primaryDark = Color(0xFF1D4ED8);

  // Semantic colors
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF06B6D4);

  // Neutral colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color transparent = Colors.transparent;

  // Background colors
  static const Color background = Color(0xFFF8FAFC);
  static const Color backgroundDark = Color(0xFF0F172A);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF1E293B);

  // Text colors
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color textInverse = Color(0xFFFFFFFF);

  // Border and divider colors
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderDark = Color(0xFF334155);
  static const Color divider = Color(0xFFE2E8F0);

  // Shadow colors
  static const Color shadow = Color(0xFF00000014);
  static const Color shadowDark = Color(0xFF0000003D);

  // Input colors
  static const Color inputBackground = Color(0xFFF1F5F9);
  static const Color inputBackgroundDark = Color(0xFF334155);

  // Card colors
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color cardBackgroundDark = Color(0xFF1E293B);

  // Status colors
  static const Color pending = Color(0xFFF59E0B);
  static const Color approved = Color(0xFF10B981);
  static const Color rejected = Color(0xFFEF4444);
  static const Color cancelled = Color(0xFF6B7280);

  // Barter specific colors
  static const Color barterPrimary = Color(0xFF059669);
  static const Color barterSecondary = Color(0xFF0D9488);
  static const Color escrow = Color(0xFF7C3AED);
  static const Color cash = Color(0xFF16A34A);

  // Payment provider colors
  static const Color papara = Color(0xFF00D4AA);
  static const Color iyzico = Color(0xFF1A1A1A);
  static const Color tosla = Color(0xFF0066CC);
  static const Color paytr = Color(0xFF8B5CF6);

  // Social colors
  static const Color facebook = Color(0xFF1877F2);
  static const Color google = Color(0xFF4285F4);
  static const Color apple = Color(0xFF000000);
}
